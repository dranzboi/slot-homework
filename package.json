{
  "name": "homework",
  "private": true,
  "scripts": {
    "start": "lerna exec -- npm run start",
    "migrations": "lerna exec --npm run migrations"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.2.3"
  }
}
